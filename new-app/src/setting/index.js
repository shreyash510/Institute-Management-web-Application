// Axios interceptors are functions that Axios calls for every request.You can use interceptors to
//  transform the request before Axios sends it, or transform the response before Axios returns the
// response to your code. 

export const AppConfigs = {
    apiBaseUrl: "http://localhost:8000",
  };
     